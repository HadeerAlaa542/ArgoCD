apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  selector:
    app: myapp
  ports:
  - port: 6060          #srvc port 
    protocol: TCP
    targetPort: 6060    #he port on the Pod (inside the container) where the app is listening.
  
